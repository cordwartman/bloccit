<h1>Edit Sponsored Post</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidlines for sponsored posts</p>
    <ul>
      <li>Make sure it doesn't rhyme.</li>
      <li>Use the letter "A".</li>
      <li>Make sure it costs money!</li>
    </ul>
  </div>
  <div class="col-md-8">
      <%= form_for [@sponsoredpost.topic, @sponsoredpost] do |f| %>
        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title, class: 'form-control', placeholder: "Enter sponsored post title" %> 
        </div>
        <div class="form-group">
          <%= f.label :body %>
          <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter sponsored post body" %>
        </div>
        <div class="form-group">
          <%= f.label :price %>
          <%= f.number_field :price, rows: 8, class: 'form-control', placeholder: "Enter sponsored post price" %>
        </div>
        <div class="form-group">
          <%= f.submit "Save", class: 'btn btn-success' %>
        </div>
      <% end %>
  </div>
</div>